{
  "name": "storewatch",
  "private": true,
  "version": "1.0.0",
  "description": "INE mock-store product price tracker",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "headed": "npm run headed --prefix backend",
    "record": "npm run record --prefix backend",
    "test": "npm test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
